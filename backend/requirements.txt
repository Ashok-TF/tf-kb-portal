# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Database ---
SQLAlchemy==2.0.36

# --- File text extraction ---
pypdf==5.1.0                # PDF
python-docx==1.1.2          # Word (.docx)
python-pptx==1.0.2          # PowerPoint (.pptx)
openpyxl==3.1.5             # Excel (.xlsx)
Pillow==11.1.0              # Images (open/inspect)
pytesseract==0.3.13         # Image OCR (requires Tesseract binary installed)

# --- Vector math (local fallback store) ---
numpy==2.2.1

# --- Optional production providers (only needed when enabled via env) ---
# Uncomment / install these when you switch to real embeddings + Pinecone:
openai==1.59.6              # EMBEDDING_PROVIDER=openai
pinecone==5.4.2             # VECTOR_STORE=pinecone
